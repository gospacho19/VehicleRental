<UserControl
    x:Class="LuxuryCarRental.Views.PaymentInfoView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="500"
    d:DesignWidth="700">

    <UserControl.Resources>
        <!-- BooleanToVisibility converter (already in place) -->
        <BooleanToVisibilityConverter x:Key="BoolToVis" />

        <!-- A simple style that disables any Button when its SelectedCard is null.
             We will use this to gray out the Delete button if nothing is selected. -->
        <Style x:Key="DisableWhenNoSelection" TargetType="Button">
            <Setter Property="IsEnabled" Value="True"/>
            <Style.Triggers>
                <!-- If SelectedCard is null, IsEnabled → False -->
                <DataTrigger Binding="{Binding SelectedCard}" Value="{x:Null}">
                    <Setter Property="IsEnabled" Value="False"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Debug status -->
            <RowDefinition Height="Auto"/>
            <!-- SavedCards list -->
            <RowDefinition Height="Auto"/>
            <!-- [Add] [Delete] buttons -->
            <RowDefinition Height="Auto"/>
            <!-- Spacer -->
            <RowDefinition Height="Auto"/>
            <!-- Add/Edit panel -->
            <RowDefinition Height="Auto"/>
            <!-- Error message -->
            <RowDefinition Height="*"   />
            <!-- filler -->
        </Grid.RowDefinitions>

        <!-- 1) Debug TextBlock (optional) -->
        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,12"
            Foreground="DarkBlue"
            FontStyle="Italic"
            Text="PanelOpen: {Binding IsAddingOrEditingCard}" />

        <!-- 2) Saved Cards ListBox -->
        <ListBox
            Grid.Row="1"
            ItemsSource="{Binding SavedCards}"
            SelectedItem="{Binding SelectedCard, Mode=TwoWay}"
            DisplayMemberPath="Nickname"
            Height="100"
            Margin="0,0,0,12" />

        <!-- 3) Add / Delete Card Buttons -->
        <StackPanel
            Grid.Row="2"
            Orientation="Horizontal"
            HorizontalAlignment="Right"
            Margin="0,0,0,12">

            <!-- Add Card -->
            <Button
                Content="Add Card"
                Command="{Binding BeginAddCardCommand}"
                Width="80"
                Margin="0,0,12,0" />

            <!-- Delete Card: disabled if no card is selected -->
            <Button
                Content="Delete Card"
                Command="{Binding DeleteCardCommand}"
                Width="80"
                Style="{StaticResource DisableWhenNoSelection}" />
        </StackPanel>

        <!-- 4) Spacer -->
        <Separator Grid.Row="3" Margin="0,0,0,12" />

        <!-- 5) Add/Edit Card Panel (same as before) -->
        <Border
            Grid.Row="4"
            Padding="12"
            BorderBrush="Gray"
            BorderThickness="1"
            Background="#FFF9F9F9"
            Visibility="{Binding IsAddingOrEditingCard, Converter={StaticResource BoolToVis}}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <!-- Card Number -->
                    <RowDefinition Height="Auto"/>
                    <!-- Expiry -->
                    <RowDefinition Height="Auto"/>
                    <!-- CVV -->
                    <RowDefinition Height="Auto"/>
                    <!-- Save / Cancel Buttons -->
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Card Number -->
                <TextBlock
                    Text="Card Number:"
                    Grid.Row="0"
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Margin="0,0,8,8" />
                <TextBox
                    Text="{Binding NewCardNumber, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    Grid.Row="0"
                    Grid.Column="1"
                    Margin="0,0,0,8" />

                <!-- Expiry -->
                <TextBlock
                    Text="Expiry (MM/YY):"
                    Grid.Row="1"
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Margin="0,0,8,8" />
                <TextBox
                    Text="{Binding NewExpiry, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    Grid.Row="1"
                    Grid.Column="1"
                    Margin="0,0,0,8" />

                <!-- CVV -->
                <TextBlock
                    Text="CVV:"
                    Grid.Row="2"
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Margin="0,0,8,8" />
                <TextBox
                    Text="{Binding NewCvv, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    Grid.Row="2"
                    Grid.Column="1"
                    Margin="0,0,0,8" />

                <!-- Save / Cancel Buttons -->
                <StackPanel
                    Grid.Row="3"
                    Grid.Column="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,8,0,0">
                    <Button
                        Content="Save"
                        Command="{Binding SaveCardCommand}"
                        Width="80"
                        Margin="0,0,8,0" />
                    <Button
                        Content="Cancel"
                        Command="{Binding CancelCardCommand}"
                        Width="80" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- 6) Error Message -->
        <TextBlock
            Grid.Row="5"
            Text="{Binding ErrorMessage}"
            Foreground="Red"
            Margin="0,8,0,0" />
    </Grid>
</UserControl>
