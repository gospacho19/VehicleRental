<!-- CartView.xaml -->
<UserControl
    x:Class="LuxuryCarRental.Views.CartView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    xmlns:vm="clr-namespace:LuxuryCarRental.ViewModels"
    d:DesignHeight="450"
    d:DesignWidth="800">

    <!-- DataContext will be set in code-behind via DI -->
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- DataGrid showing CartItems -->
        <DataGrid
            Grid.Row="0"
            ItemsSource="{Binding Items}"
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            IsReadOnly="True"
            RowHeight="100"
            HeadersVisibility="Column">
            <DataGrid.Columns>
                <!-- 1) Vehicle Image Column -->
                <DataGridTemplateColumn Header="Image" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Image
                                Source="{Binding Vehicle.ImageSource}"
                                Width="80"
                                Height="80"
                                Stretch="UniformToFill"
                                Margin="4"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- 2) Vehicle Make -->
                <DataGridTextColumn
                    Header="Make"
                    Binding="{Binding Vehicle.Make}"
                    Width="*"/>

                <!-- 3) Vehicle Model -->
                <DataGridTextColumn
                    Header="Model"
                    Binding="{Binding Vehicle.Model}"
                    Width="*"/>

                <!-- 4) Subtotal -->
                <DataGridTextColumn
                    Header="Subtotal"
                    Binding="{Binding Subtotal, StringFormat=C}"
                    Width="Auto"/>

                <!-- 5) Actions (Remove button) -->
                <DataGridTemplateColumn Header="Actions" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button
                                Content="Remove"
                                Command="{Binding DataContext.RemoveCommand,
                                                  RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}"
                                Padding="4,2"
                                Margin="4"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Bottom bar: Total, Clear, and Proceed to Checkout -->
        <StackPanel
            Grid.Row="1"
            Orientation="Horizontal"
            HorizontalAlignment="Right"
            Margin="0,10,0,0">
            <TextBlock
                Text="Total: "
                FontWeight="Bold"
                VerticalAlignment="Center"/>
            <TextBlock
                Text="{Binding Total, StringFormat=C}"
                FontWeight="Bold"
                Margin="0,0,20,0"
                VerticalAlignment="Center"/>

            <Button
                Content="Clear Cart"
                Command="{Binding ClearCommand}"
                Margin="0,0,10,0"/>

            <Button
                Content="Proceed to Checkout"
                Command="{Binding DataContext.ShowCheckoutCmd,
                          RelativeSource={RelativeSource AncestorType=Window}}"
                Margin="0,0,10,0"/>

        </StackPanel>
    </Grid>
</UserControl>
