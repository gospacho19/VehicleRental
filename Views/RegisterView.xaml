<!-- LuxuryCarRental/Views/RegisterView.xaml -->
<UserControl
    x:Class="LuxuryCarRental.Views.RegisterView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="450"
    d:DesignWidth="800">

    <StackPanel Margin="20" Width="300">

        <!-- Username -->
        <TextBlock Text="Username:"/>
        <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />

        <!-- Password -->
        <TextBlock Text="Password:" Margin="0,10,0,0"/>
        <PasswordBox x:Name="PasswordBox"
                     PasswordChanged="PasswordBox_PasswordChanged" />

        <!-- Confirm Password -->
        <TextBlock Text="Confirm Password:" Margin="0,10,0,0"/>
        <PasswordBox x:Name="ConfirmPasswordBox"
                     PasswordChanged="ConfirmPasswordBox_PasswordChanged" />

        <!-- Full Name -->
        <TextBlock Text="Full Name:" Margin="0,10,0,0"/>
        <TextBox Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}" />

        <!-- Driver License # -->
        <TextBlock Text="Driver License #" Margin="0,10,0,0"/>
        <TextBox Text="{Binding DriverLicenseNumber, UpdateSourceTrigger=PropertyChanged}" />

        <!-- Email -->
        <TextBlock Text="Email:" Margin="0,10,0,0"/>
        <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" />

        <!-- Phone -->
        <TextBlock Text="Phone:" Margin="0,10,0,0"/>
        <TextBox Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}" />

        <!-- Error Message -->
        <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,10,0,0"/>

        <!-- Buttons -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Register"
                    Command="{Binding RegisterCommand}"
                    Margin="0,0,10,0"/>
            <Button Content="Cancel"
                    Command="{Binding CancelCommand}" />
        </StackPanel>
    </StackPanel>
</UserControl>
