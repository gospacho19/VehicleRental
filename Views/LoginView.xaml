<UserControl
    x:Class="LuxuryCarRental.Views.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Margin="16">
        <StackPanel Orientation="Vertical" Width="300">
            <TextBlock Text="Username:"/>
            <TextBox Text="{Binding Username, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Text="Password:" Margin="0,12,0,0"/>
            <PasswordBox PasswordChanged="PasswordBox_PasswordChanged" />

            <CheckBox Content="Remember Me"
                      IsChecked="{Binding RememberMe, Mode=TwoWay}" Margin="0,12,0,0"/>

            <Button Content="Log In" Command="{Binding LoginCommand}" Margin="0,20,0,0"/>
            <Button Content="Register" Command="{Binding NavigateToRegisterCommand}" Margin="0,8,0,0"/>
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,12,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
