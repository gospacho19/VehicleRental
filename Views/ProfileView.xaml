<!-- ProfileView.xaml -->
<UserControl
    x:Class="LuxuryCarRental.Views.ProfileView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock 
            Text="My Profile"
            FontSize="20"
            FontWeight="Bold"
            Grid.Row="0"
            Margin="0,0,0,12"/>

        <StackPanel Grid.Row="1" Orientation="Vertical" VerticalAlignment="Top" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="Full Name:" Width="120" VerticalAlignment="Center"/>
                <TextBox
                    Text="{Binding FullName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    Width="220"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="Driver License:" Width="120" VerticalAlignment="Center"/>
                <TextBox
                    Text="{Binding DriverLicenseNumber, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    Width="220"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="Email:" Width="120" VerticalAlignment="Center"/>
                <TextBox
                    Text="{Binding Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    Width="220"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Phone:" Width="120" VerticalAlignment="Center"/>
                <TextBox
                    Text="{Binding Phone, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    Width="220"/>
            </StackPanel>
        </StackPanel>

        <Button
            Grid.Row="2"
            Content="Save Profile"
            Command="{Binding SaveCommand}"
            HorizontalAlignment="Right"
            Padding="8,4"/>
    </Grid>
</UserControl>
