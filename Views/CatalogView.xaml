<UserControl x:Class="LuxuryCarRental.Views.CatalogView"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
            xmlns:local="clr-namespace:LuxuryCarRental.Converters" 
            xmlns:viewmodels="clr-namespace:LuxuryCarRental.ViewModels" 
            d:DataContext="{d:DesignInstance Type=viewmodels:CatalogViewModel}"
            mc:Ignorable="d"
            d:DesignHeight="450" d:DesignWidth="800">

   <UserControl.Resources>
       <local:ImagePathToBitmapConverter x:Key="ImagePathToBitmapConverter"/>
   </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- header -->
            <RowDefinition Height="*"/>
            <!-- scrollable catalog -->
        </Grid.RowDefinitions>

        <!-- 1) Header -->
        <TextBlock Text="CATALOG" 
               FontSize="24" 
               FontWeight="Bold"
               Margin="0,0,0,16"
               Grid.Row="0"/>

        <!-- 2) ScrollViewer around the items -->
        <ScrollViewer Grid.Row="1"
                  VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled"
                  CanContentScroll="True">
            <ItemsControl ItemsSource="{Binding Vehicles}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" 
                       ItemWidth="220" 
                       ItemHeight="260"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#DDD" BorderThickness="1" CornerRadius="4" Padding="8" Margin="8">
                            <StackPanel Width="200">
                                <Image Source="{Binding ImagePath}"
                       Width="200" Height="120"
                       Stretch="UniformToFill"
                       Margin="0,0,0,8"/>
                                <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,0,0,4"/>
                                <TextBlock Text="{Binding DailyRate.Amount, StringFormat=C}" FontSize="14" Foreground="DarkGreen"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
