<Window
    x:Class="LuxuryCarRental.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:LuxuryCarRental.ViewModels"
    xmlns:views="clr-namespace:LuxuryCarRental.Views"
    mc:Ignorable="d"
    Title="Luxury Car Rental"
    Height="450"
    Width="800">

    <!-- 1) ViewModel→View DataTemplates -->
    <Window.Resources>
        <!-- Catalog -->
        <DataTemplate DataType="{x:Type vm:CatalogViewModel}">
            <views:CatalogView />
        </DataTemplate>

        <!-- Cart -->
        <DataTemplate DataType="{x:Type vm:CartViewModel}">
            <views:CartView />
        </DataTemplate>

        <!-- Checkout -->
        <DataTemplate DataType="{x:Type vm:CheckoutViewModel}">
            <views:CheckoutView />
        </DataTemplate>

        <!-- Confirmation -->
        <DataTemplate DataType="{x:Type vm:ConfirmationViewModel}">
            <views:ConfirmationView />
        </DataTemplate>

        <!-- Deals -->
        <DataTemplate DataType="{x:Type vm:DealsViewModel}">
            <views:DealsView />
        </DataTemplate>

        <!-- Category -->
        <DataTemplate DataType="{x:Type vm:CategoryViewModel}">
            <views:CategoryView />
        </DataTemplate>

        <!-- Profile -->
        <DataTemplate DataType="{x:Type vm:ProfileViewModel}">
            <views:ProfileView />
        </DataTemplate>

        <!-- PaymentInfo -->
        <DataTemplate DataType="{x:Type vm:PaymentInfoViewModel}">
            <views:PaymentInfoView />
        </DataTemplate>

        <!-- Login -->
        <DataTemplate DataType="{x:Type vm:LoginViewModel}">
            <views:LoginView />
        </DataTemplate>

        <!-- Register -->
        <DataTemplate DataType="{x:Type vm:RegisterViewModel}">
            <views:RegisterView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:VehicleDetailViewModel}">
            <views:VehicleDetailView />
        </DataTemplate>


        <!-- MyDeals (if you have a MyDealsViewModel/MyDealsView) -->
        <!--
        <DataTemplate DataType="{x:Type vm:MyDealsViewModel}">
            <views:MyDealsView />
        </DataTemplate>
        -->
    </Window.Resources>

    <DockPanel>
        <!-- 2) Navigation bar bound to MainViewModel commands -->
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="5">
            <Button Content="Catalog"      Command="{Binding ShowCatalogCmd}"      Margin="2"/>
            <Button Content="Categories"   Command="{Binding ShowCategoryCmd}"     Margin="2"/>
            <Button Content="Cart"         Command="{Binding ShowCartCmd}"         Margin="2"/>
            <Button Content="Checkout"     Command="{Binding ShowCheckoutCmd}"     Margin="2"/>
            <Button Content="Confirm"      Command="{Binding ShowConfirmationCmd}"  Margin="2"/>
            <Button Content="Deals"        Command="{Binding ShowDealsCmd}"        Margin="2"/>
            <Button Content="Payment Info" Command="{Binding ShowPaymentInfoCmd}"  Margin="2"/>
            <Button Content="Profile"      Command="{Binding ShowProfileCmd}"      Margin="2"/>
        </StackPanel>

        <!-- 3) Content area: shows whichever VM is CurrentViewModel -->
        <ContentControl Content="{Binding CurrentViewModel}" />
    </DockPanel>
</Window>
